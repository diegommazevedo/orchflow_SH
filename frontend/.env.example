# ── OrchFlow Frontend — Variáveis de ambiente ─────────────────────────────────
# Copie para .env.local (dev) ou configure no painel do Vercel (produção)
# NÃO commite .env.local (já está no .gitignore do Vite)

# ── URL do backend ────────────────────────────────────────────────────────────
# Em desenvolvimento: deixe vazio (Vite proxy cuida do /api)
# Em produção: URL pública do Railway SEM barra no final
VITE_API_URL=https://orchflow-production.railway.app
